<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">
            jdbc:mysql://localhost:3306/clothing_store?useSSL=false&amp;allowPublicKeyRetrieval=true&amp;serverTimezone=UTC</property>
        <property name="connection.username">root</property>
        <property name="connection.password">1234</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">5</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="current_session_context_class">thread</property>

        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Annotated classes -->
        <mapping class="com.shopapp.model.Vaitro" />
        <mapping class="com.shopapp.model." />
        <mapping class="com.shopapp.model.DanhMuc" />
        <mapping class="com.shopapp.model.NhaCungCap" />
        <mapping class="com.shopapp.model.NguoiDung" />
        <mapping class="com.shopapp.model.SanPham" />
        <mapping class="com.shopapp.model.RolePermissions" />
        <mapping class="com.shopapp.model.TonKho" />
        <mapping class="com.shopapp.model.KhachHang" />
        <mapping class="com.shopapp.model.DonHang" />
        <mapping class="com.shopapp.model.ChiTietDonHang" />
        <mapping class="com.shopapp.model.ThanhToan" />
        <!-- We can add other entity classes here as we create them -->
    </session-factory>
</hibernate-configuration>