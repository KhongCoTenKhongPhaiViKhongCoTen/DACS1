<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">
            jdbc:mysql://localhost:3306/dacs1?useSSL=false&amp;allowPublicKeyRetrieval=true&amp;serverTimezone=UTC
            </property>
        <property name="connection.username">root</property>
        <property name="connection.password">1234</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">5</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="current_session_context_class">thread</property>

        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Annotated classes -->
        <mapping class="com.shopapp.entity.Vaitro" />
        <mapping class="com.shopapp.entity.DanhMuc" />
        <mapping class="com.shopapp.entity.NhaCungCap" />
        <mapping class="com.shopapp.entity.NguoiDung" />
        <mapping class="com.shopapp.entity.Quyen" />
        <mapping class="com.shopapp.entity.SanPham" />
        <mapping class="com.shopapp.entity.RolePermissions" />
        <mapping class="com.shopapp.entity.TonKho" />
        <mapping class="com.shopapp.entity.KhachHang" />
        <mapping class="com.shopapp.entity.DonHang" />
        <mapping class="com.shopapp.entity.ChiTietDonHang" />
        <mapping class="com.shopapp.entity.ThanhToan" />
        <!-- We can add other entity classes here as we create them -->
    </session-factory>
</hibernate-configuration>